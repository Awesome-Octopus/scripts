clear all

%% Creates a matrix of spins along the z axis

for i=1:250
    Spins.degrees(i,:) = [0,90,1];
end

Spins.Video = 'y'

Pulse.direction = 'y';
Pulse.angle = 90;
Pulse.delay = 0.002;

[Spins.degrees Spins.frame] = pulse(Spins,Pulse);

[Spins.degrees Spins.frame] = pulse(Spins,Pulse);