clear all

for i=1:1
    Spins.degrees(i,:) = [0,90,1];
end

Spins.Tone = 0.000219;
Spins.Ttwo = 0.00000115;
Spins.offset = linspace(-1000000,1000000,1);

Sim.length = 0.000001;
Sim.inc = Sim.length/100;
Sim.delay = 0.05;

Spins.degrees = evolution(Spins,Sim);